import React from 'react';
import styles from '../../css/components/panels/MainPanel.module.css';

export const MainPanel = (props) => {
    return <section className={styles.mainPanel} {...props}></section>;
}